# MCP Server HTTP port
quarkus.http.port=8888

# Enable CORS
quarkus.http.cors.enabled=true

# PetClinic REST API base URL (legacy Spring app runs on port 9966)
quarkus.rest-client.petclinic-api.url=http://localhost:9966/petclinic
